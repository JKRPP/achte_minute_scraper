version: "3.8"

services:
  web:
    image: nginx:alpine
    container_name: motion-website
    ports:
      - "8085:80"
    volumes:
      - /home/user/motion-site:/usr/share/nginx/html:ro
    restart: unless-stopped
